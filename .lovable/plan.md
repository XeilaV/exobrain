Ajustar el margen lateral de zoom en `src/components/GraphView.tsx` para que, en mobile (`size.w < 640`), el padding de la caja delimitadora al hacer zoom a una rama pase de `4px` a `1px`. En escritorio se mantiene en `12px`. Se verifica en el preview con viewport móvil para confirmar que la rama desplegada ocupa casi todo el ancho de la pantalla sin cortarse.